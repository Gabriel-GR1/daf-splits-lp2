# Imagens da LP2

Coloque nesta pasta a imagem de fundo principal com exatamente este nome:

`lp2-bg.png`

O arquivo `styles.css` já está configurado para carregar automaticamente:

`./assets/images/lp2-bg.png`

Não é necessário alterar HTML, CSS ou JavaScript depois do upload da imagem.
